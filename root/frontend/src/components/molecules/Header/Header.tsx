import { Logo } from "../../atoms/Logo/Logo";
import { HeaderContainer } from "./Header.styles";

export const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};
